# PostgreSQL (obrigatório — ver plano.md §2). Não usar SQLite em produção.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/hubagendor

# Backend
PORT=3333
CLIENT_ORIGIN=http://localhost:5173
SESSION_SECRET=troque-esta-chave-em-producao-com-min-32-chars

# Uploads (S3-compatível ou pasta local em dev). O Postgres guarda só metadados.
OBJECT_STORAGE_URL=
OBJECT_STORAGE_BUCKET=hubagendor-dev
